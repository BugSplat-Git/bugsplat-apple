.DS_Store
xcuserdata
.build
build/
# The native framework is built by scripts/build-native.sh (or downloaded from a release); never committed.
Frameworks/
native/
*.xcframework.zip
Tools/symbol-upload-macos
